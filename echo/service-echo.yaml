# Source: echo
apiVersion: v1
kind: Service
metadata:
  name: echo
  labels:
    app.kubernetes.io/name: echo
    noa.re/discover: enabled
  annotations:
    teleport.dev/name: echo
    teleport.dev/port: http
  namespace: echo
spec:
  selector:
    app.kubernetes.io/name: echo
  ports:
  - name: http
    port: 80
    targetPort: http
