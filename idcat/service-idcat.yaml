# Source: idcat
apiVersion: v1
kind: Service
metadata:
  name: idcat
  namespace: idcat
spec:
  type: ClusterIP
  selector:
    app: idcat
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
